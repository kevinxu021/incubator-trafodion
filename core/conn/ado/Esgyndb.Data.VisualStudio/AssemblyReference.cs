namespace Esgyndb.Data.VisualStudio
{
    /// <summary>
    /// This is an empty class that can be used to obtain a reference to the 
    /// HP.Data.VisualStudio assembly without requiring references to the 
    /// base classes which might not be available on machines without
    /// Visual Studio installed.
    /// 
    /// Usage: <code>Assembly.GetAssembly(typeof(HP.Data.VisualStudio.AssemblyReference))</code>
    /// </summary>
    public class AssemblyReference
    {
    }
}
